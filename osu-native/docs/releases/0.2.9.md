# AimMod osu 0.2.9

- Refresh recent Coaching plays automatically and when returning to the page, while preserving the selected play.
- Keep submitted scores available during temporary connection failures and fix missing practice-set actions for stable plays.
- Preserve stable scoring information when combining local and submitted score history.
- Show calculated PP on the PP targets page even when there is not enough history to estimate pass chance. These values are labelled "PP if passed".
